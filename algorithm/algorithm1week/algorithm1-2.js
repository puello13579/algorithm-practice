const fruits = ["사과", "바나나", "파인애플"];

const newFruits = [];

console.log(fruits[fruits.length - 1]);

newFruits.push(fruits[fruits.length - 1]);

//fruits.length값을 사용하면 그 숫자값은 항상 인덱스 값보다 1이 더 많은 렝스값이므로 거기서 -1을 해주면 항상 마지막 값이 된다.//
